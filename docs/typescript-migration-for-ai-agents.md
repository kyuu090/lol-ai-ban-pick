# TypeScript Migration Plan

この文書は旧入口です。長大化による生成AIのトークン消費を避けるため、TypeScript 移行計画は Phase 別に分割しました。

以後は次を読む。

1. `docs/ts-magiration/README.md`
2. `docs/ts-magiration/common.md`
3. 対象 Phase の `docs/ts-magiration/phase-XX-*.md`
4. 対象 Phase の `docs/ts-magiration/save-data/phase-XX-*-save-data.md`

TypeScript 移行の作業証跡は、従来の `docs/refactoring-for-ai-agents-save-data.md` ではなく `docs/ts-magiration/save-data/` 配下へ保存する。
